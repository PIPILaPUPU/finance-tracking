# Frontend

React + Vite + TypeScript UI for Finance Tracker.

## Run

```bash
npm install
npm run dev
```

Mock auth: any email + password (≥8 chars). Demo credentials are prefilled on the login screen.

Backend is not connected yet — data lives in memory/localStorage (auth session only).
